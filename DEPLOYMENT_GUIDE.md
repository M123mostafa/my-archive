# 🚀 راهنمای جامع آنلاین‌سازی سایت و فعال‌سازی ۱۰ گیگابایت فضای ابری رایگان

این راهنما به شما نشان می‌دهد چطور سایت خود را به‌صورت کاملاً رایگان آنلاین کنید و **۱۰ گیگابایت فضای ابری Cloudflare R2** همراه با **دیتابیس ابری Supabase** به آن متصل کنید تا فایل‌های شما روی هر دستگاهی (گوشی، لپ‌تاپ و ...) قابل دسترسی باشند.

---

## 📍 گام ۱: ساخت دیتابیس ابری رایگان (Supabase)

1. وارد سایت [Supabase.com](https://supabase.com) شوید و با اکانت گیت‌هاپ یا ایمیل ثبت‌نام کنید.
2. روی **New Project** کلیک کنید و یک پروژه جدید بسازید (نام دلخواه و رمز دیتابیس وارد کنید).
3. بعد از ساخته شدن پروژه، به بخش **Project Settings -> Database** بروید.
4. آدرس **Connection String (URI)** یا **Transaction pooler** را کپی کنید. فرمت آن به شکل زیر است:
   `postgres://postgres:[YOUR-PASSWORD]@db.xxxx.supabase.co:5432/postgres`
   *(به جای `[YOUR-PASSWORD]`، رمزی که موقع ساخت پروژه گذاشتید را بنویسید)*.

---

## 📍 گام ۲: ساخت فضای ابری رایگان ۱۰ گیگابایت (Cloudflare R2)

1. وارد سایت [Cloudflare.com](https://dash.cloudflare.com) شوید و لاگین کنید.
2. از منوی سمت چپ روی **R2 Object Storage** کلیک کنید.
3. روی **Create Bucket** کلیک کرده و نام باکت خود را بنویسید (مثلاً `learning-archive-files`).
4. پس از ساخت باکت، به صفحه اصلی **R2** برگردید و از پنل سمت راست روی **Manage R2 API Tokens** کلیک کنید.
5. روی **Create API Token** کلیک کنید:
   - **Permissions**: گزینه **Admin Read & Write** را انتخاب کنید.
   - روی **Create API Token** کلیک کنید.
6. مقادیر زیر را که نمایش داده می‌شود کپی و ذخیره کنید:
   - **Account ID** (از آدرس مرورگر یا صفحه اصلی R2 قابل کپی است)
   - **Access Key ID**
   - **Secret Access Key**

---

## 📍 گام ۳: آنلاین کردن سایت روی Vercel (رایگان)

1. پروژه خود را روی **GitHub** آپلود کنید (یا از طریق Vercel CLI اقدام کنید).
2. وارد سایت [Vercel.com](https://vercel.com) شوید و **Add New -> Project** را بزنید و ریپازیتوری گیت‌هاپ خود را انتخاب کنید.
3. در بخش **Environment Variables**، متغیرهای زیر را وارد کنید:

| نام متغیر (Key) | مقدار (Value) |
|---|---|
| `DATABASE_URL` | لینک دیتابیس Supabase (از گام ۱) |
| `R2_ACCOUNT_ID` | اکانت آیدی Cloudflare (از گام ۲) |
| `R2_ACCESS_KEY_ID` | کلید اکسس R2 (از گام ۲) |
| `R2_SECRET_ACCESS_KEY` | کلید سکرت R2 (از گام ۲) |
| `R2_BUCKET_NAME` | نام باکت شما (مثلاً `learning-archive-files`) |

4. روی **Deploy** کلیک کنید! 

🎉 **تبریک!** سایت شما پس از حدود ۱ دقیقه آنلاین می‌شود و بالا سمت راست علامت **`🟢 Cloud Sync (R2 + Postgres)`** فعال خواهد شد.
از این پس هر فایلی آپلود کنید مستقیم در Cloudflare R2 ذخیره شده و روی تمام دستگاه‌ها به صورت آنلاین و همگام نمایش داده می‌شود.
